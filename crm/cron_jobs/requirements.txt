# requirements.txt

Django==4.2.16  # Django framework (adjust version as needed)
graphene-django==3.2.0  # For GraphQL integration
gql==3.4.0  # For executing GraphQL queries in Python scripts
requests==2.31.0  # Required by gql for HTTP transport
django-crontab==0.7.1  # For managing cron jobs within Django